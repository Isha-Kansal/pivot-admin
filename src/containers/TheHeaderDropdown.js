import React from "react";
import { Button } from "reactstrap";

const TheHeaderDropdown = () => {
  return (
    <div>
      <Button color="bordered" className="logout-btn ml-2">
        Logout
      </Button>
    </div>
  );
};

export default TheHeaderDropdown;
